#ifndef __RAWLOG_H
#define __RAWLOG_H

/**
 * RAWLOG is a mode that is used to evaluate raw sensor data, using the same
 * code/library and algorithms as the core sensor. It's used for comparisons between
 * sensors.
 */

void rawlog();

#endif
